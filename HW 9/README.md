Reflection:

I used the provided example as a basis for my code. I followed its formatting to create my image and text arrays and found the basic animate, fade-in, fade-out, etc. syntax from it. I had a hard time figuring out how to switch between the different shapes, and wasn't quite sure what should go in the constructor of the shape array, so I consulted AI for that section, and got the add and remove class parts from it. The rest was a bit of an amalgamation, and I reused parts that worked across multiple sections.
